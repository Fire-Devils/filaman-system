<style is:global>
  .print-page { overflow: hidden; }
  .preview-container { flex: 1; overflow: auto; padding: 40px; background: var(--bg-soft); display: flex; flex-direction: column; align-items: center; gap: 24px; }
  .preview-container.is-label-sheet-mode { align-items: flex-start; }
  .label-count { color: var(--text-muted); font-size: 0.85rem; }
  .preview-container.is-label-sheet-mode .label-count { align-self: flex-start; left: 50%; position: sticky; transform: translateX(-50%); }
  .preview-zoom-bar {
    position: sticky; top: 0; left: 50%; z-index: 10;
    align-self: center;
    transform: translateX(-50%);
    padding: 8px 12px;
    border: 1px solid var(--border); border-radius: var(--radius-sm);
    background: color-mix(in srgb, var(--bg-elevated) 92%, transparent);
    box-shadow: 0 8px 24px rgba(0,0,0,0.12); backdrop-filter: blur(10px);
  }
  .preview-container.is-label-sheet-mode .preview-zoom-bar { align-self: flex-start; }
  .preview-zoom-btn { width: 28px; height: 28px; border-radius: var(--radius-sm); font: inherit; line-height: 1; }
  .preview-zoom-btn:hover { color: var(--accent); }
  .preview-zoom-slider { width: 150px; }
  .preview-zoom-label { min-width: 44px; font-size: 0.8rem; font-weight: 600; }
  .label-preview {
    background: white !important;
    box-shadow: 0 4px 24px rgba(0,0,0,0.18);
    color: black !important;
  }
  .label-mfr-text,
  .label-designation,
  .label-color-name,
  .label-hex-code,
  .label-spool-id,
  .label-id-corner { color: black !important; }

</style>
