<style is:global>
  .print-page {
    overflow-x: auto;
    overflow-y: hidden;
  }

  .print-sidebar { transition: width 0.2s ease; }
  .print-sidebar.sidebar-wide { width: 530px; }
  .back-link { transition: opacity var(--transition); }

  .fm-input {
    font-family: var(--font-sans);
    transition: border-color var(--transition), box-shadow var(--transition);
  }

  .fm-input:focus {
    outline: none;
    border-color: var(--accent);
    box-shadow: 0 0 0 3px var(--focus-ring);
  }

  .fm-checkbox-group { transition: background var(--transition); }

  .fm-checkbox-group input {
    width: 18px;
    height: 18px;
    accent-color: var(--accent);
    cursor: pointer;
  }

  .fm-btn {
    letter-spacing: 0.2px;
    transition: transform var(--transition), box-shadow var(--transition), background var(--transition);
  }

  .preview-container {
    flex: 1;
    min-width: 320px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    background: var(--bg-soft);
  }

  .preview-zoom-bar {
    padding: 8px 20px 6px;
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
    flex-shrink: 0;
  }

  .preview-zoom-btn {
    width: 26px;
    height: 26px;
    border-radius: 6px;
    font-size: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: background var(--transition), border-color var(--transition);
  }

  .preview-zoom-btn:hover {
    background: var(--accent);
    color: var(--btn-primary-text);
  }

  .preview-zoom-reset { font-size: 0.85rem; }

  .preview-zoom-slider {
    flex: 1;
    max-width: 160px;
    cursor: pointer;
  }

  .preview-zoom-label {
    font-size: 0.75rem;
    min-width: 38px;
    font-variant-numeric: tabular-nums;
  }

  .preview-scroll-area {
    flex: 1;
    display: flex;
    align-items: safe center;
    justify-content: safe center;
    padding: 40px;
    overflow: auto;
  }

  .label-preview {
    box-shadow: var(--shadow-lg);
    border: 1px dashed #ccc;
  }
</style>
